{
  "name": "sideline-ai-chat",
  "version": "1.0.0",
  "type": "module", 
  "scripts": {
    "build": "echo 'No build step required, dependencies are installed.'" // ใส่คำสั่งที่ไม่ทำอะไร แต่ทำให้ Netlify ไม่ติด Error
  },
  "dependencies": {
    "@google/genai": "^0.1.0"
  }
}