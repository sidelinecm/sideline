{
  "name": "sideline-ai-chat-proxy",
  "version": "1.0.0",
  "description": "A proxy for Google Gemini API to hide the API key.",
  "main": "api/gemini.js",
  "dependencies": {
    "@google/genai": "0.1.0" // สำคัญ: ไม่มีเครื่องหมาย ^ (Caret) แล้ว
  }
}