{
  "name": "sideline-ai-chat",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "echo 'No build step required, running dependencies only'"
  },
  "dependencies": {
    "@google/genai": "^0.1.0"
  }
}